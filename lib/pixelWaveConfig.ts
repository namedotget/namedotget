/** Quantized columns/rows in the transition shader (tweak performance vs crispness). */
export const PIXEL_WAVE_GRID_CELLS = 500;

/** Normalized thickness of the glowing voxel band (shader space). */
export const PIXEL_WAVE_BAND = 0.055;

/** Total transition duration when motion is allowed (ms). */
export const PIXEL_WAVE_DURATION_MS = 920;

/** Reduced motion duration (ms). */
export const PIXEL_WAVE_DURATION_REDUCED_MS = 220;
