/** Quantized columns/rows in the transition shader (lower = chunkier voxels). */
export const PIXEL_WAVE_GRID_CELLS = 50;

/** Normalized thickness of the glowing voxel band (shader space). */
export const PIXEL_WAVE_BAND = 0.092;

/** Secondary outer halo width multiplier. */
export const PIXEL_WAVE_HALO = 0.22;

/** Total transition duration when motion is allowed (ms). */
export const PIXEL_WAVE_DURATION_MS = 1500;

/** Reduced motion duration (ms). */
export const PIXEL_WAVE_DURATION_REDUCED_MS = 1000;
